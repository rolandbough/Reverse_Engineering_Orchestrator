# Core MCP Server Dependencies
# ADR Note: Using MCP Python SDK for protocol implementation
mcp>=1.16.0  # Model Context Protocol Python SDK (updated by ida-pro-mcp)
pydantic>=2.0.0
typing-extensions>=4.8.0

# Reverse Engineering Tool MCP Servers
ida-pro-mcp>=1.4.0  # Existing MCP server for IDA Pro (reference/alternative)

# Tool Detection
psutil>=5.9.0  # For process detection
winreg>=0.0.0; sys_platform == "win32"  # Windows registry access

# Logging and Utilities
python-dotenv>=1.0.0  # Environment variable management

# Development Dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
mypy>=1.5.0

# Optional: Tool-specific dependencies (installed separately)
# idapython  # IDA Pro Python API (installed via IDA Pro)
# ghidra_bridge  # Ghidra Python bridge (installed separately)

# Phase 2: Visual Analyzer
opencv-python>=4.8.0  # Computer vision
mss>=9.0.0  # Fast screen capture
numpy>=1.24.0  # Array operations
# Optional: OCR support
# pytesseract>=0.3.10  # Text extraction (requires Tesseract OCR installed)

